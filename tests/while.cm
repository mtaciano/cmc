/* FUNCIONA */

void main(void)
{
  int x;
  x = 0;

	while (x < 4) {
    x = x + 2;
  }

  while (9 >= x) {
    x = x + 5;
  }

  while (x <= 24) {
    x = x + 10;
  }

  while (x != 35) {
    x = x + 1;
  }

  output(x);
}
