/* O objetivo desse teste é verificar se é possível
 * usar uma quantidade arbitrária de `if` dentro de
 * outros `if`
 */

void main(void) {
    int x;
    x = 99;

    if (x > 5) {
        if (x > 12) {
            if (x < 50) {
                x = 42;
            } else {
                x = 999;
            }
        }
    }

    output(x); /* 999 */
}
