void main(void)
{
	int x;
	x = 1;

	while (x < 10) {
    x = x + 1;
  }

  while (x > 20) {
    x = x + 1;
  }

  while (x <= 20) {
    x = x + 1;
  }

  while (x >= 20) {
    x = x + 1;
  }

  while (x != 20) {
    x = x + 1;
  }

  output(x);
}
